% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bcgpfit-class.R, R/bcgpfitpred-class.R
\name{summary,bcgpfit-method}
\alias{summary,bcgpfit-method}
\title{\code{summary} method for a \code{bcgpfit} object}
\usage{
\S4method{summary}{bcgpfit}(object, pars, quantiles = c(0.025, 0.25, 0.5, 0.75, 0.975), ...)

\S4method{summary}{bcgpfit}(object, pars, quantiles = c(0.025, 0.25, 0.5, 0.75, 0.975), ...)
}
\arguments{
\item{object}{a bcgpfit object}

\item{pars}{a character vector specifying the parameters to summarize}

\item{quantiles}{a numeric vector specifying the desired quantiles for each
parameter}
}
\description{
This gives a summary of the posterior draws contained in a \code{bcgpfit}
object

This gives a summary of the posterior draws contained in a \code{bcgpfit}
object
}
\examples{


data_sim <- bcgpsims(composite = TRUE, stationary = FALSE, noise = FALSE,
                     d = 2, decomposition = TRUE)

model <- bcgpmodel(x = data_sim@training$x, y = data_sim@training$y,
                   composite = TRUE, stationary = FALSE, noise = FALSE)
fit <- bcgp_sampling(model, scaled = TRUE, cores = 4, n_mcmc = 500,
                     burnin = 200)

fit
print(fit, pars = c("beta0", "w", "rhoG", "rhoL"), digits_summary = 3)
summary(fit)



data_sim <- bcgpsims(composite = TRUE, stationary = FALSE, noise = FALSE,
                     d = 2, decomposition = TRUE)

model <- bcgpmodel(x = data_sim@training$x, y = data_sim@training$y,
                   composite = TRUE, stationary = FALSE, noise = FALSE)
fit <- bcgp_sampling(model, scaled = TRUE, cores = 4, n_mcmc = 500,
                     burnin = 200)

fit
print(fit, pars = c("beta0", "w", "rhoG", "rhoL"), digits_summary = 3)
summary(fit)

}
